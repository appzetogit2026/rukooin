✅ Background Color

➡️ First row → 4th color ONLY
(soft teal, clean, premium)

Approx hex:

BACKGROUND → #8CCFCB

✅ Cards / Sections Color

➡️ Second row → 4th color ONLY
(deep teal, rich, premium)

Approx hex:

SURFACE / CARD → #004F4D

🧠 THEME PHILOSOPHY (VERY IMPORTANT)

Background → calm, airy, light

Cards & sections → dark, solid, premium

Contrast → natural, eye-friendly

Look & feel → modern hotel / travel app

No gradients.
No extra colors.
Very clean & professional.

🧩 CENTRALIZED TAILWIND THEME (BEST WAY)
✅ tailwind.config.js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        background: "#8CCFCB", // first row 4th
        surface: "#004F4D",    // second row 4th
        accent: "#14B8A6",     // CTA / highlight
        textLight: "#E6F7F6",
        muted: "#9FDAD7",
      },
      borderRadius: {
        xl: "16px",
        "2xl": "20px",
      },
    },
  },
  plugins: [],
};

🖥️ GLOBAL APP BACKGROUND
<div className="min-h-screen bg-background text-surface">
  <AppLayout />
</div>

🧾 CARD / SECTION STANDARD
<div className="
  bg-surface
  text-textLight
  rounded-2xl
  p-6
  shadow-xl
">
  Section / Card Content
</div>

🏨 HOTEL CARD (PREMIUM LOOK)
<motion.div
  whileHover={{ y: -6 }}
  transition={{ type: "spring", stiffness: 200 }}
  className="
    bg-surface
    rounded-2xl
    overflow-hidden
    shadow-2xl
    border border-white/10
  "
>
  <img src={image} className="h-48 w-full object-cover" />

  <div className="p-4 text-textLight">
    <h3 className="text-lg font-semibold">{hotelName}</h3>
    <p className="text-sm text-muted">{location}</p>
    <p className="mt-2 text-accent font-bold">₹{price}/night</p>
  </div>
</motion.div>

🔘 BUTTON (CTA)
<button className="
  bg-accent
  text-surface
  px-6
  py-3
  rounded-xl
  font-semibold
  hover:scale-105
  transition
">
  Book Now
</button>
